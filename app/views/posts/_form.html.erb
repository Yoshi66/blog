<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='field'>
    <%= f.label :position %><br>
    <%= f.text_field :position %>
  </div>

  <%= fields_for(@article) do |u| %>
    <div class="field">
      <%= u.label :title %><br>
      <%= u.text_field :title %>
    </div>
    <div class="field">
      <%= u.label :subtitle %><br>
      <%= u.text_field :subtitle %>
    </div>
    <div class="field">
      <%= u.label :content %><br>
      <%= u.text_field :content %>
    </div>
  <% end %>
    <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
